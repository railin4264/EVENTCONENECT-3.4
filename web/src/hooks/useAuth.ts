'use client';

// This module re-exports the canonical auth hook to avoid duplicated contexts.
export { useAuth } from '@/components/providers/AuthProvider';

export default undefined as unknown as never;